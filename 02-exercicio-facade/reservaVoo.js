class ReservaVoo {
    fazerReserva(origem, destino, data) {
        // Lógica complexa para reservar um voo
        console.log(`🛫Reserva de voo de ${origem} para ${destino} na data ${data} realizada com sucesso.`);
    }
}

module.exports = ReservaVoo;
