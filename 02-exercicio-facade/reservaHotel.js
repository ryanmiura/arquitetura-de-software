class ReservaHotel {
    fazerReserva(cidade, dataEntrada, dataSaida) {
        // Lógica complexa para reservar um hotel
        console.log(`🏢Reserva de hotel em ${cidade} de ${dataEntrada} até ${dataSaida} realizada com sucesso.`);
    }
}

module.exports = ReservaHotel;
